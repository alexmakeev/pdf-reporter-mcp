services:
  pdf-reporter:
    build: .
    container_name: pdf-reporter-mcp
    restart: unless-stopped
    ports:
      - "${PORT:-17002}:17002"
    volumes:
      - pdf-output:/app/output
      - pdf-temp:/app/temp
    environment:
      - NODE_ENV=production
      - TRANSPORT=sse
      - PORT=17002
    networks:
      - dokploy-network

volumes:
  pdf-output:
  pdf-temp:

networks:
  dokploy-network:
    external: true
